Tag the following article with {min_keywords} to {max_keywords} tags.

You MUST choose only from this fixed list of allowed tags:
{allowed_tags}

Title: {title}
Category: {category}
Summary: {summary}

Rules:
- Pick the tags that best describe what the article is actually about.
- Use at most {max_keywords} tags; fewer is better than inaccurate.
- Copy tag names exactly as written in the allowed list.
- Respond with only a comma-separated list of tags, nothing else.
